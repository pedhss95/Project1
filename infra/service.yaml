apiVersion: v1
kind: Service
metadata:
  name: fastapi-service
  namespace: mentor-project
spec:
  selector:
    app: fastapi-app
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8008